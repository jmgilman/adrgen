import { Given, Then } from "cucumber";

Then(/^the (.*) config file is created$/, function() {

});
Given(/^there is not any config file$/, function() {

});
Given(/^there is a config file created with this configuration$/, function() {

});