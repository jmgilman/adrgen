import { Then, When } from "cucumber";

When(/^the init command is executed$/, function() {

});
When(/^the create command is executed$/, function() {

});
When(/^the user executes the list command$/, function() {

});
Then(/^the user see the result on the screen:$/, function() {

});
When(/^the user executes the list command with the filter "([^"]*)"$/, function() {

});
When(/^the user specify the (.*) relation with the target ADR with the (.*) id$/, function() {

});